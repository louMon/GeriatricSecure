<%= form_with(model: pathology, local: true) do |form| %>
  <% if pathology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pathology.errors.count, "error") %> prohibited this pathology from being saved:</h2>

      <ul>
      <% pathology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idpathology %>
    <%= form.text_area :idpathology, id: :pathology_idpathology %>
  </div>

  <div class="field">
    <%= form.label :cie10_code %>
    <%= form.text_area :cie10_code, id: :pathology_cie10_code %>
  </div>

  <div class="field">
    <%= form.label :pathology_name %>
    <%= form.text_area :pathology_name, id: :pathology_pathology_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :pathology_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
